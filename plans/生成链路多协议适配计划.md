# 生成链路多协议适配计划（anthropic / gemini / openai-responses）

## 记录时间

2026-09-24

## 背景与定位

设置页自《设置页优化计划》起支持四档协议选择（`openai` / `openai-responses` / `anthropic` / `gemini`），但仅停在配置层：拉模型列表与测试连接多协议可用（`models-client.ts`），**生成链路（Agent Loop / 流式 tool_calls / 要素提取 / 压缩摘要）只实现 openai 兼容协议**（`agent/llm.ts` 固定请求 `/chat/completions`；`AgentLlmInput` 无 protocol 字段，设置页选的协议根本传不到生成层）。用户选 anthropic / gemini 后能拉列表、能测试连接通过，但真正写作走不通。《当前进度》「待开始 → LLM 协议扩展」已预留本计划位置。

参考系为 DeepSeek Harness（dsh）的 llm 分层（`packages/llm/`）：**provider 中立词汇 + 适配器接缝 + 线协议实现**。关键调研结论：

- dsh 的 Agent 循环只认识中立消息/内容块/流事件词汇（`dsh-llm` 包），每个 provider 路由一个适配器把线格式翻译进中立词汇；
- dsh 自己不手写 anthropic/gemini 线协议，而是外包给 `@earendil-works/pi-ai` 库（多 provider 目录 + 线协议实现），`llm-pi-ai` 适配器只做事件翻译；
- **pi-ai 搬不了**：Node-only（传输层用 Node 内置模块 + 五个云 SDK，无 browser 导出条件），dsh 自己的 webworker 运行时都把它整体 stub 掉（`notImplementedFail`）——dsh 的浏览器部署同样没有多协议。NovAI 是纯浏览器应用（直连 fetch + DEV 代理），线协议适配器必须自写 fetch + SSE，好在 openai 一档已有同款模式（`agent/llm.ts`），其余协议照此平行新增；
- pi-ai 的各协议实现源码（纯 JS 可读）作为线格式参考资料对照，不装依赖。

## 已拍板决策

### 用户已拍板（2026-09-24 对话确认）

1. **三协议全做**：anthropic + gemini + openai-responses 一次收尾，不拆两期。
2. **思考流 UI 一并做**：聊天区做折叠式思考过程展示（连 DeepSeek 的 reasoning_content 也从未展示过，本期补上）。
3. **真机验证按 DeepSeek 支持面走**：用户在 `~/note/test-novel` 配置了 DeepSeek key。经查证 DeepSeek 官方支持 openai 兼容与 **Anthropic 兼容端点**（`https://api.deepseek.com/anthropic`），因此 openai（回归）与 anthropic 两档真机可测；gemini 与 openai-responses DeepSeek 不支持，本期以 wire-level mock 测试兜底，待用户有 key 再补真机。

### 方案设计拍板（本计划定稿即生效，审阅时可推翻）

4. **分层照 dsh 裁剪，不引入 pi-ai**：
   - 中立词汇沿用现有 `AgentMessage`（本就 provider 无关），只扩两处：`AgentLlmEvent` 增 `reasoning-delta` 事件；`AgentAssistantResponse` 与 assistant 消息持久化增可选 `reasoning?: string` 字段。dsh 的块级 `StreamChunk` 词汇（块索引 / usage / block-start-end）不搬——NovAI 的 UI 消费不到，保持最小词汇。
   - 适配器接口 `ProtocolAdapter`：负责 URL 解析、鉴权与浏览器头、请求体构造（中立消息 → 线格式）、SSE 解析（线事件 → 中立事件）、工具调用累积、错误分类。openai 适配器整体继承现有实现（含非流式 fallback 与 tool-call 容错诊断）；其余协议首版**不做**非流式 fallback。
   - 协议表分发：`{ openai → OpenAiChatAdapter, anthropic → …, gemini → …, 'openai-responses' → … }`，W1 先注册 openai 一档，其余波次补齐。
5. **finishReason 归一化**：`'stop' | 'tool_calls' | 'length'` 三值（anthropic `end_turn→stop`、`tool_use→tool_calls`、`max_tokens→length`；gemini `STOP→stop`、`MAX_TOKENS→length`、其余归 error；responses `completed→stop`、`function_call→tool_calls`）。wire 原文进 diagnostics。`query.ts` 的判定逻辑不变。
6. **anthropic `max_tokens` 必填**：适配器缺省 8192（压缩摘要路径显式传值不受影响）；openai-responses 映射 `max_output_tokens`；gemini 映射 `generationConfig.maxOutputTokens`。
7. **请求不回传 reasoning（只收不发）**：各适配器构造请求时丢弃历史消息里的 reasoning 内容。anthropic 侧不开 `thinking` 参数（不开就不触发「思考块必须回传」约束）；gemini 侧若线格式在 functionCall part 上带 `thoughtSignature`，适配器将其存进 `AgentToolCall` 可选附加字段随消息持久化，后续请求原样拼回（规避 Gemini 2.5 函数调用思考签名校验），不进 UI 不进工具 schema。
8. **要素提取同链受益**：`llm/client.ts::streamChatCompletion` 薄化为协议层入口（单一入口、随 protocol 分发），删除其 openai 直连实现；`elements/extractor-llm.ts` 自动跟随协议。FIM 输入补全（`completion-client.ts`，DeepSeek 专用）不动。
9. **思考流 UI 形态拍死**（用户可见）：
   - 流式中：assistant 消息顶部「思考中…」折叠块自动展开，实时滚动显示 reasoning 文本；**正文 text-delta 开始输出后自动收起**；
   - 收起态与历史消息：显示「已深度思考（N 字）」，点击可展开/收起，历史默认收起；
   - 思考文本按纯文本 `whitespace-pre-wrap` 渲染（不走 markdown 渲染器），字号小于正文、灰色调；
   - 无 reasoning 的消息（含全部旧会话）完全不渲染该块，旧行为零变化。
10. **协议链路**：`novel.config.json llm.protocol`（已存在）→ services `LlmConfigView`（已有 protocol 字段）→ `ChatTurnInput.config.llm` → query → `AgentLlmInput.protocol`（新增，缺省 `'openai'`）。设置页 UI 零改动（协议选择已存在）。
11. **测试模式**：沿用 `vi.stubGlobal('fetch')` + 手写 SSE fixture 的 wire-level 单测（每适配器三类断言：请求构造、SSE 全事件路径、错误分类），不引 MSW 等新依赖。
12. **CORS/直连**：anthropic 适配器请求带 `anthropic-dangerous-direct-browser-access: true`（对齐 models-client 现状）；全部适配器统一走 `shared.ts` 的 `appendProxyHeader`（DEV 代理已协议无关）。`api.deepseek.com/anthropic` 的浏览器 CORS 行为未知，W2 真机时验证；不通则按降级方案处理。
13. **`llm.ts` 对外签名不变**：`streamAgentCompletion(input, onEvent)` 签名与返回值不动（`query.ts` / `compaction.ts` 零改动），只换内部分发；改动全部收在 core 内部，app 侧除思考流外零感知。

## 本期范围

### 做

#### W1 协议层地基 + openai 适配器迁移（NovAI core）

改动点：

1. 新建 `packages/core/src/core/llm/protocol/` 模块：
   - `types.ts`：`ProtocolAdapter` 接口、`AgentLlmEvent` 增 `{ type: 'reasoning-delta'; text: string }`、`AgentAssistantResponse` 增 `reasoning?: string`；
   - `openai-chat.ts`：现 `agent/llm.ts` 的流式实现整体迁入（非流式 fallback、tool-call 容错与 dropped 诊断、空闲超时、abort 语义全部随迁），并**补齐 `delta.reasoning_content`（DeepSeek reasoner）解析** → `reasoning-delta` 事件 + `reasoning` 字段；
   - `resolve.ts`：协议表（本期先注册 openai 一档，其余波次补齐）。
2. `agent/llm.ts` 重构为薄分发层：`streamAgentCompletion` 按 `input.protocol` 分发到适配器，签名不变；`AgentLlmInput` 增 `protocol?: ModelProtocol`（缺省 `'openai'`）。
3. 协议链路贯通：核对 app 设置 → `ChatTurnInput.config.llm.protocol` → `AgentLlmInput.protocol` 每一跳（services `LlmConfigView` 已有字段，补齐传递断点；app 侧若漏传 protocol 则补传）。
4. `llm/client.ts::streamChatCompletion` 薄化为协议层入口（增 `protocol?` 入参），删除 openai 直连实现；`extractor-llm.ts` 传入协议。
5. `agent/llm.test.ts` 迁移为 `openai-chat` 适配器测试（原断言全保留），新增 reasoning_content 解析用例。

W1 门禁：`pnpm test` 全绿 + `pnpm typecheck` 干净 + 真机回归（test-novel 项目、openai 协议、deepseek-flash 正常写作一轮含工具调用）。

降级方案：真机回归失败且属环境问题（网络/key）时，以 wire 测试 + 用户复核兜底。

#### W2 anthropic 适配器

改动点：

1. `protocol/anthropic.ts`：
   - `POST {base}/v1/messages`；请求头 `x-api-key` + `anthropic-version: 2023-06-01` + `anthropic-dangerous-direct-browser-access: true`，走 `appendProxyHeader`；baseUrl 兼容 `https://api.anthropic.com` 与 `…/v1` 两种写法（复用 models-client 同款归一逻辑）；
   - 请求体：system prompt 放顶层 `system`；user → `{role:'user', content:[{type:'text'}]}`；assistant → text block + `tool_use` block（id/name/input）；工具结果 → user 消息 `tool_result` block（`tool_use_id` + 文本 content）；tools → `{name, description, input_schema}`；`max_tokens` 必填（缺省 8192）；`stream: true`；
   - SSE 解析：`message_start` / `content_block_start`（text | thinking | tool_use）/ `content_block_delta`（`text_delta`→delta、`thinking_delta`→reasoning-delta、`input_json_delta`→累积）/ `content_block_stop` / `message_delta`（stop_reason 归一）/ `message_stop`；`ping` 事件忽略；
   - 错误分类：HTTP 非 2xx 读 `error.message`（401/403 鉴权、429 限流、529/5xx 服务端）。
2. wire 测试：请求构造断言（system 位置 / tool_result 映射 / max_tokens 缺省与显式）、SSE fixture 全事件路径（文本 + 思考 + 工具调用混合流）、错误分类。
3. 真机验收：test-novel 临时改 `protocol: 'anthropic'`、`baseUrl: 'https://api.deepseek.com/anthropic'`（key 不变）→ 正常写作一轮（含一次工具调用与一次文件写入），验证后改回。

W2 门禁：全绿 + typecheck + 真机清单通过。

降级方案：若 `api.deepseek.com/anthropic` 浏览器直连被 CORS 拦截 → 以 mock 断言 + DEV 代理路径验证兜底，真机直连留用户复核清单（此时注意区分「适配器错」与「CORS 错」：代理通而直连不通即 CORS）。

#### W3 gemini 适配器

改动点：

1. `protocol/gemini.ts`：
   - `POST {base}/v1beta/models/{model}:streamGenerateContent?alt=sse`；请求头 `x-goog-api-key`；baseUrl 归一（兼容带/不带 `/v1beta` 尾缀）；
   - 请求体：system prompt → `systemInstruction`；消息 → `contents[{role:'user'|'model', parts:[…]}]`（assistant 文本 → `{text}`、工具调用 → `{functionCall:{name,args}}`、工具结果 → user `{functionResponse:{name, response}}`）；tools → `tools:[{functionDeclarations:[{name, description, parameters}]}]`；`generationConfig.maxOutputTokens` 可选透传；
   - SSE data 行解析：`candidates[0].content.parts`（`{text}`→delta、`{text, thought:true}`→reasoning-delta、`{functionCall}`→toolCalls）；`finishReason` 归一（STOP→stop、MAX_TOKENS→length、SAFETY/RECITATION 等→error）；functionCall part 带 `thoughtSignature` 时存入 `AgentToolCall` 附加字段并在构造后续请求时原样拼回（决策 7）。
2. wire 测试（mock only，本协议无真机）：请求构造（systemInstruction / functionResponse / thoughtSignature 回传）、SSE fixture（混合 parts 流、thought 文本分流）、finishReason 归一与 SAFETY error 路径。

W3 门禁：`pnpm test` 全绿 + `pnpm typecheck` 干净。

真机：无 key，进未验证清单，待用户有 Gemini key 后按 W2 同款步骤复核。

#### W4 openai-responses 适配器

改动点：

1. `protocol/openai-responses.ts`：
   - `POST {base}/responses`；Bearer 鉴权；**stateless 全量 input 模式**（不使用 `previous_response_id`，每次请求带完整历史，会话状态由 NovAI 自持）；
   - 请求体：system prompt → `instructions`；历史 → `input[]`（user → `{type:'message', role:'user', content:[{type:'input_text'}]}`、assistant 文本 → `{type:'message', role:'assistant', content:[{type:'output_text'}]}`、工具调用 → `{type:'function_call', call_id, name, arguments}`、工具结果 → `{type:'function_call_output', call_id, output}`）；tools → `{type:'function', name, description, parameters}`；`max_output_tokens` 可选透传；`stream: true`；
   - SSE 解析：`response.output_text.delta`→delta、`response.reasoning_summary_text.delta`→reasoning-delta（若出现）、`response.function_call_arguments.delta` 累积、`response.completed`（归一 finishReason，取 output items 组装 toolCalls）、`response.failed` / `error` 事件；其余事件类型忽略。
2. wire 测试（mock only）：请求构造（instructions / function_call_output / stateless 全量）、SSE fixture（文本 + function_call 流、failed 路径）。

W4 门禁：`pnpm test` 全绿 + `pnpm typecheck` 干净。

真机：无 key，进未验证清单（OpenAI /responses 或任意兼容网关）。

#### W5 思考流 UI（core 事件链 + app 组件）

改动点：

1. core：reasoning-delta 事件沿现有流事件链透传到 app（session-driver → store → 消息视图；挂点沿现有 delta 链平行增设，实现时自选并在交付说明记录）；assistant 消息持久化带 `reasoning` 字段（W1 已定词汇）。
2. app：
   - 新增 `components/chat/ReasoningCollapse.vue`：按决策 9 形态实现（流式自动展开 + 「思考中…」呼吸态、正文开始后自动收起、「已深度思考（N 字）」点击展开/收起、纯文本 pre-wrap 小号灰字）；
   - `MessageItem.vue` assistant 文本消息顶部挂载（`reasoning` 存在才渲染）；
   - ChatPanel 流式状态接线（streaming 消息实时更新 reasoning 文本）。
3. 逻辑单测：收起/展开状态机（流式→正文开始→收起；历史→默认收起）、无 reasoning 不渲染。
4. 真机验收：test-novel 模型临时切 `deepseek-reasoner`（openai 协议）→ 观察思考流实时展示与自动收起；anthropic 端点（`api.deepseek.com/anthropic`）同理验证 thinking 块路径；验证后改回 deepseek-flash。
5. browser-use 冒烟：折叠交互与历史默认收起形态目检。

W5 门禁：全绿 + typecheck + `pnpm build` + browser-use 冒烟 + 真机清单。

#### W6 收尾结项

1. 文档：`当前进度.md`「待开始 → LLM 协议扩展」条目移至已完成；`types/ai.ts` 与 `types/project.ts` 的「仅配置层」注释更新为真实支持面；本计划施工日志汇总、未验证清单定稿。
2. 复查 test-novel 配置已还原（protocol/baseUrl/model 复原）。

W6 门禁：`pnpm test` + `pnpm typecheck` 全绿 + 文档同步完成。

### 明确不做清单

- **pi-ai 库引入**（Node-only，dsh 浏览器运行时也 stub，本期自写适配器）；
- **usage/token 用量统计**（dsh TokenUsage 词汇不搬，NovAI 无消费方）；
- **embedding / rerank 协议适配**（openai 兼容够用，独立面板）；
- **FIM 输入补全协议**（`completion-client.ts` DeepSeek 专用，不动）；
- **搜索 provider（perplexity/exa）线协议**（固定格式，非用户可配协议）；
- **OAuth / credentials / login 体系**（dsh credentials 不搬）；
- **DEV 代理改造**（已协议无关）；
- **anthropic `thinking` 参数开启与思考块回传**（本期 reasoning 只收不发）；
- **压缩摘要携带 reasoning**（摘要输入不进思考内容）；
- **非流式 fallback 泛化到新协议**（仅 openai 适配器保留该能力）；
- **设置页协议选择 UI 改动**（已存在，本期只接通生成）。

### 迁移条款

- `novel.config.json` 的 `llm.protocol` 字段已存在，无迁移；
- `AgentLlmInput.protocol` 可选缺省 `'openai'`，旧调用方零改动；
- 会话持久化 assistant 消息 `reasoning` 为可选字段，旧会话无此字段 → UI 不渲染思考块，零迁移；
- gemini `thoughtSignature` 附加字段可选，旧数据无影响。

**结论：本期无任何旧数据/旧配置迁移项。**

### 新旧机制替代表

| 旧机制 | 去向 |
| :--- | :--- |
| `agent/llm.ts` streamAgentCompletion 的 openai 直连实现 | **替换**：实现迁入 `protocol/openai-chat.ts`，`llm.ts` 只留协议分发（对外签名不变） |
| `llm/client.ts` streamChatCompletion 的 openai 直连实现 | **替换**：薄化为协议层入口 |
| `agent/llm.test.ts` | **迁移**：改为 openai-chat 适配器测试（断言保留） |
| 设置页协议选择（配置层） | **并存接通**：UI 不动，生成层开始消费 |

### 验收与降级矩阵

| 项 | 自动化验证 | 真机验证 | 降级方案 |
| :--- | :--- | :--- | :--- |
| openai 协议（迁移回归） | wire 测试 + 全量既有测试 | test-novel · deepseek-flash · 写作一轮含工具调用 | — |
| anthropic 适配器 | wire 测试（请求/SSE/错误） | `api.deepseek.com/anthropic` + deepseek key · 写作一轮 | CORS 不通 → DEV 代理验证 + 用户复核 |
| gemini 适配器 | wire 测试（请求/SSE/归一） | 无 key | 进未验证清单，待用户复核 |
| openai-responses 适配器 | wire 测试（请求/SSE/failed） | 无 key | 进未验证清单，待用户复核 |
| 思考流 UI | 状态机逻辑单测 + 既有测试 | deepseek-reasoner 双端点（openai + anthropic） | — |
| 要素提取跟随协议 | 随协议层入口的既有测试 | 未单独真机（同协议链路） | 依赖生成链路同款验证 |

### 文档同步计划

- 本计划：`docs/plans/README.md` 索引状态流转（待开始 → 进行中 → 已完成），施工日志按格式随手追加（日期 + 波次 + 提交 hash + 验收结论 + 偏差记录）。
- `docs/project/当前进度.md`：开工时把「LLM 协议扩展」从待开始移入进行中锚点，结项时落已完成区。
- `packages/core/src/types/ai.ts`、`types/project.ts` 的「仅配置层」注释在 W6 更新。
- 提交纪律：长任务分阶段提交（AGENTS.md 长任务例外条款）——每波门禁（测试全绿 + typecheck + 文档同步）通过即提交一次并报 hash；scope 用 core 侧 `project`、UI 侧 `ui`、文档 `docs`；docs 子模块先提交再推进父仓库指针；不 push。

## 施工日志

### 2026-09-24 W1 协议层地基 + openai 适配器迁移完工（`773e1a5`）

**验收结论**：`pnpm test` 394 项全绿（新增 2 项：reasoning 流式增量、reasoning 非流式 fallback 提取）+ `pnpm typecheck` 干净；真机回归通过——经临时 vitest 文件（跑完即删）读 `~/note/test-novel` 的 DeepSeek 配置直连真实 API：① 纯文本流式生成 13 个 delta 且拼接与最终 content 一致、finishReason=stop；② 工具调用轮 finishReason=tool_calls 且产出 `ListDirectory` 调用（id/input 合法）。协议链路核对：`novel.config.json` 整体解析为 `ProjectConfig`（无字段白名单），`llm.protocol` 经 query / compaction / 要素提取三处调用点全部接通。

**偏差记录**：

1. **非流式 reasoning 提取的测试路径修正**：原计划直接断言非流式响应体的提取，实际 openai 兼容线路上带 body 的 JSON 响应会进流式解析分支（`data:` 行不存在、产出空内容），非流式提取只发生在 fallback 的 `readJsonResponse` 路径——测试改为经「流式丢函数名 → 非流式 fallback」链路覆盖。对既有语义的认知修正，产品行为无变化。
2. **真机回归手段**：以临时测试文件直连 API 验证适配器线格式与流式语义（对外签名不变，UI 链路由全量测试覆盖），未起浏览器走完整写作链——后续波次真机沿用此手段。
3. **resolve 对未实现协议的报错**：计划未写明文案，实现为「尚未实现「xxx」协议的生成链路，请在设置中改用已支持的协议」（W2-W4 落地前，anthropic/gemini/openai-responses 配置触发生成会得到明确报错而非静默 openai），属实现完备性补齐。

### 2026-09-24 W2 anthropic 适配器完工（`35d1d5e`）

**验收结论**：`pnpm test` 400 项全绿（新增 6 项 wire 测试）+ `pnpm typecheck` 干净；真机全过——① CORS 预检实测：`api.deepseek.com/anthropic` 放行浏览器直连（OPTIONS 预检回显 Origin、允许 content-type/x-api-key/anthropic-version/anthropic-dangerous-direct-browser-access 全部所需头），计划中的 CORS 未知数消除；② Node 侧经适配器真实流式调用（临时测试文件跑完即删）：纯文本轮 delta 拼接与 content 一致、finishReason=stop；**工具调用完整往返**——round1 模型发起 `ListDirectory` tool_use（finishReason=tool_calls、id/input 合法），round2 把 tool_result 按线格式喂回后模型正常总结目录内容收尾（stop）。

**偏差记录**：

1. **错误事件双发修正**：初版在 HTTP 非 2xx 分支与外层 catch 各发一次 error 事件，wire 测试抓出后收敛为 catch 层统一发一次（与 openai 适配器的单次语义对齐）。
2. **共享 SSE 工具 `protocol/sse.ts`**：计划未写明，实现时从 anthropic 抽出帧解析 + 空闲超时 + abort 竞速为共享模块（W3 gemini / W4 openai-responses 复用）；openai-chat 适配器保留自己的循环（其 fallback 包裹语义特殊，不动已验证代码）。
3. **isContextOverflowError 扩充**：补 anthropic「prompt is too long」与 gemini「token count exceeds the maximum」文案识别（W3 顺带生效），计划错误分类职责的完备性补齐。

### 2026-09-24 W3 gemini 适配器完工（`64b4a7f`）

**验收结论**：`pnpm test` 406 项全绿（新增 6 项 wire 测试）+ `pnpm typecheck` 干净。按计划本协议无真机 key，全部以 wire mock 兜底，进未验证清单待用户有 Gemini key 后按 W2 同款步骤复核（真机回归手段见 W1 偏差 2）。

**偏差记录**：

1. **finishReason 推导修正**：初版直接映射线上 finishReason，wire 测试抓出 gemini 语义细节——函数调用轮的线上 finishReason 也是 `STOP`（gemini 无独立原因码），直接映射会把工具轮归一成 stop。修正为「有函数调用即 tool_calls」按实际内容推导（Agent 循环以 toolCalls.length 分支，语义不受影响，但 finishReason 诊断与 parse-warning 判定需要正确值）。
2. **安全类中止按错误收场**：计划写「SAFETY 等 → error」，实现为抛错（消息含原始 finishReason）——NovAI 的 finishReason 词汇无 error 值，错误必须走事件/异常通道；安全类中止通常无可用内容，静默截断成「成功」会误导。
3. **thoughtSignature 落点**：按决策 7 存进 `AgentToolCall.thoughtSignature` 可选字段随 assistant 消息持久化，gemini 适配器构造后续请求时原样回传（wire 测试断言回传）；其余适配器与 UI 不感知。

### 2026-09-24 W4 openai-responses 适配器完工（`39407e3`）

**验收结论**：`pnpm test` 412 项全绿（新增 6 项 wire 测试，含四协议 resolve 可达性断言）+ `pnpm typecheck` 干净。协议表四档全部注册，**设置页可选的四种协议至此全部接入生成链路**。按计划本协议无真机 key，wire mock 兜底，进未验证清单（待 OpenAI /responses 或兼容网关 key）。

**偏差记录**：

1. **function_call 收集方式**：计划写「function_call_arguments.delta 累积」，实现改为从 `response.output_item.done` 的完整 item 收集（arguments 全串一次到位，避免增量累积的丢段风险）；delta 事件仍在 fixture 中存在但不再消费——Responses API 保证 output_item.done 必发，语义等价且更稳。
2. **`response.incomplete` 事件**：计划未列，实现补上（Responses API 的截断信号，映射 length）；`response.failed` 与流内 `error` 事件按计划走抛错路径。

### 2026-09-24 W5 思考流 UI 完工（`6af57cd`）

**验收结论**：`pnpm test` 420 项全绿（新增 8 项：session-driver 思考流全链路 2、store 占位/追加/防串扰 2、折叠状态机 4）+ `pnpm typecheck` + `pnpm build`（vue-tsc + vite）全过；browser-use 冒烟实测通过；真机双协议验证通过。

事件链挂点（沿正文 delta 链平行增设，按计划要求记录）：`query.ts` 转发 `reasoning-delta` → `assistant-reasoning-delta`（AgentQueryEvent）→ `session.ts` 产出 `message-reasoning-delta`（SessionEvent，与正文共享 streamingMessageId，思考先于正文分配占位 id）→ `agent-service.ts` 原样广播 AgentUiEvent → store 处理器追加 `reasoning`（无占位则建 text 空占位）→ `MessageItem.vue` 挂 `ReasoningCollapse`（thinking = streaming && 正文为空）。持久化：`AssistantTextMessage.reasoning` / `ChatMessageView.reasoning` 可选字段；**思考后直接调工具的轮次（content 空）也落一条仅带 reasoning 的气泡**。

真机清单（临时测试文件跑完即删，key 不入库；test-novel 配置文件全程未改——模型/协议覆盖在测试内存中完成，无需还原）：

1. **openai 协议**：deepseek-reasoner 经 `api.deepseek.com`，reasoning-delta x44 先于 text-delta x6，assistant 消息带 75 字 reasoning + 正文，思考位置先于正文。
2. **anthropic 协议**：deepseek-reasoner 经 `api.deepseek.com/anthropic`，thinking 块 → reasoning-delta x43 先于正文，72 字 reasoning 落盘。另经 curl 证实该端点对 reasoner 系模型**无需 thinking 请求参数**即自动输出 thinking 块（流式 `thinking_delta` 事件齐全）——适配器只收不发的取值成立。

browser-use 冒烟（内存 FS shim + evaluate 点击，同联网搜索波次的自动化适配）：① 流式时序采样——新消息「思考中」展开态（46 字 → 472 字增长），正文首个 delta 到达即翻「已深度思考」并自动收起；② 折叠交互——点击展开（aria-expanded=true、灰色小字思考文本可见）再点收起；③ 历史形态——切新会话再切回，4 个思考块（含切换前手动展开过的）全部默认收起，reasoning 随会话 JSON 落盘重载不丢；④ 视觉目检（截图 + 视觉模型核对）——收起形态（小灰字 + 箭头、位于正文上方）与展开形态（左边框竖线、max-h 滚动区、箭头旋转）均无排版异常；⑤ 思考后直接调工具的轮次独立成气泡（骰子题那轮 ReadFile 前的 1320 字思考）。

**偏差记录**：

1. **折叠手动态响应性缺陷（冒烟抓出、已修）**：状态机初版用普通闭包变量存手动展开值，组件 `expanded` 走 computed 无法追踪闭包变量 → 点击展开无反应。修正为 `ref` 支撑的 composable（`reasoning-collapse.ts`）。单测因命令式调用而通过、只有真实渲染暴露——冒烟门禁的价值实证。
2. **「无 reasoning 不渲染」的浏览器级负例不可产**：实测发现 DeepSeek 现役 `deepseek-flash` 本身即混合思考模型（curl 证实其非流式响应带 `reasoning_content`），用户两个可用模型都会思考，浏览器层无法产生「无 reasoning」模型。该负例覆盖落在：模板 `v-if="reasoning"` 守卫 + 四协议 wire 测试的「无 reasoning 字段」fixture + session-driver 第 2 轮无思考断言。
3. **冒烟环境适配（同前波，非产品缺陷）**：devtools 覆盖层拦截 playwright 点击 → evaluate 点击；evaluate 有 3s 上限 → 长轮询改 Node 侧采样循环；IAB 键盘 Enter 不达 Vue 处理器 → 发送按钮 evaluate 点击。
4. **思考中自动滚动**：计划形态未写明，实现补了思考流式期间容器自动滚到底（max-h 限高 + overflow-auto，用户可手动上翻），收起/历史态不抢滚动。

### 2026-09-24 W6 收尾结项（docs-only，无代码改动）

**验收结论**：`当前进度.md`「待开始 → LLM 协议扩展」条目移入已完成（生成链路多协议适配，2026-09-24 结项）；`types/ai.ts`（ModelProtocol）与 `types/project.ts`（ProjectConfig.llm.protocol）的「仅配置层」注释更新为真实支持面；`plans/README.md` 索引状态流转为已完成；test-novel 配置复查未改动（模型/协议覆盖全程在测试内存中完成，原配置 deepseek-flash / openai / v1 原样）。`pnpm test` 420 项全绿 + `pnpm typecheck` 干净（注释与文档改动后复跑）。

## 未验证清单（定稿）

真机验证以 DeepSeek 双协议完成（用户提供的 key，openai `/chat/completions` 与 `api.deepseek.com/anthropic`，含工具调用完整往返、思考流、CORS 预检）。以下项无 key，以 wire mock 测试兜底，待用户配齐 key 后按 W1 偏差 2 的真机手段（临时测试文件直连）复核：

1. **gemini 协议真机**：`streamGenerateContent?alt=sse` 流式、functionCall/functionResponse 往返、thought 块 + thoughtSignature 回传、安全类中止路径。复核要点：thoughtSignature 在多轮工具调用的真实回放是否被服务端接受。
2. **openai-responses 协议真机**：`/responses` SSE、function_call 完整往返、reasoning_summary（若模型输出）。复核要点：stateless 全量 input 在长会话下的行为与 `response.incomplete` 触发形态。
3. **思考流 UI 的 gemini / openai-responses 路径**：UI 链路协议无关（中立事件词汇），已由 openai / anthropic 双协议真机覆盖；gemini thought 块与 responses reasoning_summary 的 UI 呈现待上述两条真机时顺带目检。
4. **anthropic 真实 Anthropic 端点**（非 DeepSeek 兼容层）：thinking 需要显式 `thinking` 请求参数才输出的真行为、thinking block + signature 在工具往返中的回放约束（当前只收不发策略在真 Anthropic 上的兼容性）。DeepSeek 兼容层已实测无需参数自动输出。

## 结项说明

六波全部完工（W1 `773e1a5` / W2 `35d1d5e` / W3 `64b4a7f` / W4 `39407e3` / W5 `6af57cd` / W6 docs-only）。零迁移条款兑现：`llm.protocol` 为可选字段缺省 openai，旧配置与会话 JSON 无需任何迁移即兼容（reasoning / thoughtSignature 均为可选字段）。明确不做清单 11 项全部遵守，无范围蔓延。
