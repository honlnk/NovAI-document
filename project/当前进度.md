# 当前进度

最后更新:2026-09-23

## 已完成

### 基础工程与 MVP 闭环(Phase 0-2)
- 已建立产品规划、AI 功能需求、UI 设计、技术架构、v1 技术选型决策等规划文档
- 已初始化 Vue 3 + TypeScript + Vite 前端工程、Vue Router、Pinia
- 已实现本地小说项目的创建、打开、不合法目录检测与修复、标准项目目录初始化
- 已实现 `novel.config.json` 读写、`prompts/system.md` 读取与保存
- 已实现项目文件扫描、分组展示、原文预览
- 已实现 LLM / Embedding / Rerank 配置测试连接、LLM 流式生成链路、章节文件写入 `chapters/`
- 已明确 AI 模块接口与流式事件协议
- 已实现最近项目 IndexedDB 记忆、刷新后恢复、最多 8 个最近项目列表
- 已实现首页功能(项目列表、新建/打开、恢复上次、章节数与要素数「N 章 · M 个要素」)

### Agent Loop 第一版(Claude Code 参考系)
- 已完成第一版 Claude Code 风格 Agent Loop 骨架
- 已实现模型驱动的 `ReadFile / EditFile / CreateFile / RenameFile / DeleteFile / ListDirectory / FindFiles` 工具调用链路
- 已为 `ReadFile / EditFile` 接入 `readFileState` 与文件过期写入保护
- 已移除 Bash 工具占位,当前不提供通用命令执行能力
- 已实现 OpenAI-compatible `tools / tool_calls` 流式解析、工具结果回灌模型的多轮 Query 循环
- 已建立项目级 JSONL 日志系统 `.novel/logs/agent.log.jsonl`,记录 Agent Run / Query Step / 模型调用 / 工具调用 / 项目打开关闭等事件

### UI 协作接口层
- 已完成第一版 UI 协作接口层 `packages/core/src/services/*`
- 已新增运行时项目注册表,UI 不再直接持有 `FileSystemDirectoryHandle`
- 已新增 `projectStore / settingsStore / chatStore` 对 service 的状态适配
- 已迁移 `SessionTestView`、`TestLabView` 使用 stores/services
- 已确认 `packages/app/src/views` 与 `packages/app/src/stores` 不再直接 import `packages/core/src/core/*`
- 已将接口层类型整理为显式 view type

### 主工作区 UI(详见 [UI 重构计划](../plans/UI重构计划.md),R1-R7 全部落地)
- 已用 ActivityBar + CategoryPanel(章节 / 要素 / 提示词 / 对话)替换原 FileTreeSidebar
- 已将设置入口从整页路由改为模态框 SettingsModal
- 已为所有 API Key 输入框接入可复用 PasswordInput(眼睛图标切换显隐)
- 已为内容面板新增预览 / 编辑 / 原始三态分段控件、编辑模式(Ctrl/Cmd+S 保存 + dirty 标记)
- 已实现选中内容引用(预览模式选中文本生成 quote chip 透传给模型,切文件清空)
- 已实现内容面板拖拽改宽(240~720px + 双击重置 320px + localStorage 持久化)
- 已实现 `@场景` 输入框指令(筛选 + 键盘导航 + 场景 chip 双向联动)
- 已实现 `/` 斜杠命令菜单(命令注册表驱动,可扩展)、`/提取要素` 多章节多选 + 候选预览 + 确认写入 + 同名合并
- 已实现 Toast 提示、首次使用引导、文件树实时更新

### 要素体系
- 已实现第一版章节要素提取链路(人物、地点、情节、时间线、世界观)
- 已将要素提取收敛为 **LLM 唯一路径**,移除正则降级提取器
- 已新增 `elements/entities/` 标准分组,扩展 element 类型、要素提取、写入、解析、RAG metadata、检索过滤、UI 数量展示
- 已统一项目目录补齐策略:新建项目、打开/修复、恢复上次项目都补齐 `elements/entities/`

### RAG 检索
- 已将 `RagSearch` 接入 Agent Loop,模型可主动调用检索要素上下文
- 已将粗召回升级为正式 Orama 向量检索(每 projectId 一个常驻内存实例、维度不匹配降级空召回、type/lastUpdatedChapter 精确过滤、tags 多召回+本地精确兜底)
- 已将 RAG 索引状态/重建入口从设置页移到主工作区底部常驻状态栏,接入 contentHash 增量重建
- 已新增 `index-events.ts` 事件总线(独立于 AgentUiEvent)、`useIndexStore`、`IndexStatusBar.vue`(健康度指示 + stale/error 可点击重建)

### Agent 控制能力(详见 [Agent 控制能力补强计划](../plans/Agent控制能力补强计划.md),六个 Step 全部落地)
- Step 1:写工具结构化 FileChange(`extractFileChange` → `fileChange` 透传),删除文本反推逻辑
- Step 2/3:写工具执行前用户确认 + diff 预览 + 暂停/确认/拒绝,清理旧残留
- Step 4:Agent 运行中停止(LLM AbortSignal + Loop 停止信号检查 + 保留已生成内容)
- Step 5:用户即时工具约束(软约束 filterAvailableTools + 硬约束 executeAgentTool,支持「不读/不写/只读/只写当前文件」)
- Step 6:system prompt 同会话刷新(FNV-1a hash 比对,system.md 或场景提示词变化在下一轮自动反映)
- 已实现项目级累积记忆 `prompts/NovAI.md`(详见 [0004 决策](../decisions/0004-引入NovAI项目总览.md))、`/生成项目记忆` 斜杠命令

### Agent Loop 重构(参考系 Claude Code → DeepSeek Harness,详见 [Agent Loop 重构开发计划](../plans/Agent%20Loop%20重构开发计划.md) 与 [0005 决策](../decisions/0005-参考系切换至DeepSeekHarness.md))
- Stage 0:三条平行执行路径收敛为单 Loop、僵尸配置六件套清除、tool-policy 全链路删除
- Stage 1-6 全部落地(`abfcbb4`→`323a7b0`,另含代理流式修复 `1ed4ce7`):
  - `model-view.ts` 把「发给模型的消息序列」与聊天显示层彻底分离,旧会话自动迁移
  - `compaction.ts` 8 段检查点上下文压缩(阈值+溢出双触发、保留近期轮、tool 配对回退、摘要自合并、CJK 感知 token 估算:中文 1 字 ≈ 1 token)
  - `permission.ts` 范围权限(工作区内静默允许、工作区外一次性确认)
  - `spill.ts` 超长工具结果落盘(ReadFile/RagSearch >8192 字符写 `.novel/spill/`)
  - `agentMaxTurns` 可配置且超限优雅收尾
  - 流式打字机渲染(stable streamingMessageId + 光标)

### 章节格式与命名规范
- 已将章节正文格式从 `.md` 切到 `.txt`,工具层拒绝新建/移动到 `chapters/*.md`;旧 `.md` 章节仍可读取/预览、Agent 可经 `RenameFile` 迁移,但 2026-09-21(`43275d9`)起不计入章节数、不进提取列表
- 已统一章节命名为 `第NNN章-标题.txt` 并在工具层强制(`26a28d2`):`chapter-name.ts` 单一事实来源、`path.ts` 写入路径校验、`edit-file.ts` 对称强制、create/rename 重号检测、prompt/tools/defaults 文案统一
- 已实现旧项目章节批量整理工具(`136a9b0`)
- 已下线机械章节整理工具(`43275d9`,2026-09-21):`organize-service`、`OrganizeChaptersModal`、ActivityBar 正常态入口整体移除(回溯 `136a9b0` 可找回)——它是未来 AI 整理模块的前置基建,该模块暂不开发;命名规范的工具层强制保留,ActivityBar「章节整理」回到「即将推出」灰态
- 《章节格式调整计划》结项(2026-09-21):五个 Step 全部完成,索引流转至「已完成」;衍生的「生成结果结构化处理」继续留在待开始清单(自动打开变更文件、章节级元数据,未立计划)

### 对话输入框 AI 补全(详见 [对话输入框 AI 补全计划](../plans/对话输入框AI补全计划.md))
- DeepSeek FIM 流式客户端(`choices[0].text` 主读 + `delta.content` 兜底、15s 空闲超时、中断保留已生成部分)
- `completion` 独立配置分组(仿 rerank 可开关)
- `useInlineCompletion`(防抖 / AbortController / `Intl.Segmenter` 中文分词逐段接受,不支持时按字符回退)
- `GhostTextOverlay` 灰色建议覆盖层、设置页「输入补全」tab
- 计划外顺带完成输入框卡片式样式改造

### 设置页优化(详见 [设置页优化计划](../plans/设置页优化计划.md))
- 布局照抄 gpt-image-studio(BaseModal 三段式 + 左侧竖排导航 + tab 记忆 + 5 个面板组件)
- 模型配置借鉴 duet(LLM 协议选择 openai/openai-responses/anthropic/gemini + 默认地址联动 + 四面板统一获取列表/测试连接 + 用途过滤)
- 自动保存(防抖 600ms + enabled 开关立即 flush + 关闭前 flush + footer 状态指示)
- core 新增 `models-client.ts` 多协议拉取(anthropic/gemini 端点与鉴权、DashScope 自动改写 compatible-mode 修复百炼 rerank 拉列表 404)

### 要素模块缺陷补全(详见 [要素模块缺陷补全计划](../plans/要素模块缺陷补全计划.md),2026-09-23 结项)
- 五类要素默认模板落地 core 资产(`templates.ts`),提取 prompt 注入模板分节,plot 强制按事件拆分,timeline 必带「所属阶段」与「故事内时间」
- 要素 tags 由模型产出(2~5 个概括性标签),解析层容错后与兜底标记合并
- Agent 主动编写要素的一切(双轨收敛、写入合并、编号、模糊去重、整理指令)经拍板整体暂缓,待方案重新评估

### W0-W6 四份待办施工波次(详见 [实施路线图-四份待办施工波次](../plans/实施路线图-四份待办施工波次.md),2026-09-19 全完工)
- **W0 小毛病清扫**(`a1cf7b6` + `871081e`):压缩期停止归类 aborted、摘要请求补 max_tokens、数值配置钳制收口、isContextOverflowError 识别百炼文案、UI 按 sessionId 过滤 delta
- **W1 权限 + spill**(`32aac02`):写工具权限五档落 `permission.ts` + `.novel/` 防护补齐(含大小写绕过修复);ReadFile 三道闸(2000 行 / 2000 单行字符 / 50KB 字节)、`.novel/spill/` 可读化、spill 路径豁免二次 spill、7 天启动清理
- **W2 循环 core**(`9ceb4ac`):inbox 双队列纯函数(claim=next-step 抽干 + next-turn 恰 1 条)、query for→while(true)+ step 边界抽干点、steer 续命、`agentMaxTurns` 默认 0 不限、session driver 化(入队先于唤醒、首批 claim、每 turn 独立 runId、stopChatDriver 清闩锁保队列)
- **W3 账本 core**(`ac8006e`):changeLedger append-only 账本(事件驱动累积、随会话 JSON 落盘、压缩免疫)、collectChangedFiles 改读账本、结尾文字 action-summary 改造为 change-summary 结构化消息、EditFile/CreateFile output 扩展实际应用文本 + extractChangeDiff、删除 lastWrittenPath 死代码
- **W4 service/store 合并**(`9510aa1`):service 层 runTurn 拆为 enqueueMessage / updateQueuedMessage / stopAgentRun、subscribeAgentEvents 事件总线(全部事件带 sessionId)、queue-updated 全量快照广播、file-changed 载荷升级为完整账本记录、ChatSessionView 新增 changedFiles/changedFileCount/queuedMessages、store isRunning 事件驱动、QueueDock 数据通路、发送失败草稿恢复
- **W5 UI 合并**(`4dfbddd`):toolCallId 配对、chat-render 纯函数(配对/分组/steering 非组边界)、ToolCallRow / TurnProcessGroup / TurnChangesPanel / DiffLines / QueueDock 新组件、MessageItem 去头像、ChatPanel 输入解禁(Enter=排队 / Ctrl+Enter=插话)、权限档位选择器、QueueDock 挂输入框正上方
- **W6 历史工具+收尾**(`a02ffe5`):GetFileChangeHistory 只读工具(新→旧清单 + path/runId/limit 过滤)、ToolRuntime 账本只读取口、设置面板 agentMaxTurns 文案改「0 表示不限制」、min 放宽 0
- **修复批**(`0df34ae` + `687ac55` + `0bc65cb`):停止竞态修复(isRunning 改按 driver 注册表真实状态)、运行中删除会话修复(deleteSession 先 stopChatDriver + deletedSessionIds 墓碑)、agentMaxTurns 旧出厂默认 8 一次性迁移为 0、对话输入区权限档位选择器(与设置页共享同一份配置)
- **修复批 2**(`2c6701d`,用户复核六条):轮次安全阀提示独立 turn-limit kind 不再折进任务组、QueueDock 折叠计数头区分排队/插话、TurnProcessGroup 注释改口 display:none 收起后 Ctrl+F 搜不到、输入区运行中补「插话」鼠标按钮、排队消息文件快照改为消息级(每条 followup 用自己入队时的文件)、任务组 messageCount 剔除工具行与 toolCallCount 口径不叠加

## 进行中

### Element 要素体系优化计划(部分落地,继续推进)
- 已落地:`elements/entities/`、`entity` 类型、实体提取、RAG metadata、UI 数量展示
- 已由《要素模块缺陷补全计划》落地(2026-09-23):五类要素默认模板(`templates.ts`,worldbuilding 按计划不出模板)、提取 prompt 模板注入、plot 事件粒度、timeline 阶段字段、tags 模型产出
- 仍待补齐:
  - 剧情块按事件拆分:提取侧 prompt 已强化(禁止总纲式条目);写入侧维持一事件一文件
  - 时间线按阶段拆分:提取侧已带「所属阶段」字段;阶段归并为单文件依赖写入合并能力,随「Agent 编写要素」方案一并暂缓
  - 「整理要素结构」Agent 指令、Agent 编写要素行为约束:随「Agent 编写要素」方案整体暂缓(2026-09-23 拍板,方案待重新评估)

### 持续性工作
- 持续收敛 `services/types.ts`,后续按 project / agent / rag / settings 拆分接口类型
- 在真实创作任务中验证 Agent 主动 `RagSearch` 的触发率、召回质量与后续 `ReadFile(sourcePath)` 使用策略
- 真实项目验证上下文压缩摘要质量与压缩后创作连贯性(当前仅 mock 模型验证过结构)

## 待开始

### AI 能力
- 将近期章节上下文 `recentChapters` 接入当前模型驱动 Agent Loop,或明确作为工具化读取策略实现(类型已定义,当前恒为空数组)
- 要素提取的去重合并与覆盖策略、Agent 编写要素的通道与确认形态——随「Agent 编写要素」方案一并再议(2026-09-23 拍板暂缓)
- 生成结果结构化处理:自动打开变更文件、章节级元数据(计数刷新已随 `refreshTree` 回写收掉 `86af61a`;章节命名规范与旧项目批量整理工具已完成)

### RAG 能力
- 实现生成时的粗检索复用
- 完善索引过期提醒、自动后台增量重建与错误降级策略(状态栏常驻 + contentHash 手动增量重建已落地,自动后台重建与错误降级待补)
- 为高级审校预留更完整的上下文接口

### 章节整理完整形态(来自《章节命名规范与整理工具计划》升级路径,均未启动;原机械改名工具已随 `43275d9` 下线,升级路径不受影响)
- `/整理` 斜杠命令接入(slash-commands 注册表已预留扩展位)
- 章节元数据落地(字数、第几章、新建 vs 续写)
- 整理规范可配置化(命名规则 + 字数范围,脱离硬编码)
- AI 驱动的拆分 / 合并 + 衔接补写(对应原始 AI 章节整理模块的完整形态,需另立新计划)
- 整理规范作为可调教资产(对齐产品愿景)

### 工程补充
- 完善错误处理与降级策略
- 设计 Git 自动提交策略(Phase 5 基座,章节整理 / 校对 / 版本管理配套依赖)

### LLM 协议扩展
- anthropic / gemini 生成链路适配(设置页已支持协议配置,实际生成适配待另立计划)

### 文档
- 「dsh 借鉴与映射设计」文档待补(对位 0002,系统梳理 DeepSeek Harness 借鉴清单)

## 阻塞与风险

- 要素提取已收敛为 LLM 唯一路径;同名覆盖写、去重合并与 Agent 编写要素通道随方案拍板暂缓(2026-09-23),待「Agent 编写要素」方案重新评估后一并解决
- 纯前端文件系统方案依赖浏览器能力,浏览器兼容性有限
- `RagSearch` 已接入 Agent Loop,但模型是否稳定主动调用仍需要真实创作验证
- 粗召回已升级为 Orama 内存向量检索,但自动后台增量重建与错误降级策略仍未补齐,当前依赖写入后标记 stale + 用户手动重建
- 两阶段检索中的「Rerank 精排」效果需要在真实生成链路中继续验证
- 对话输入框 AI 补全为探索性功能,FIM 流式 delta 字段兼容性(代理网关改写场景)与延迟体验待真实使用验证
- 上下文压缩已落地,但摘要质量目前仅用 mock 模型验证过结构,真实模型的摘要质量与压缩后创作连贯性需真实项目验证;`conversationTokenLimit` 设置得低于保留区内容时会每轮触发压缩(预期尽力而为行为),默认 12000 不受影响
