# 当前进度

最后更新：2026-04-30

## 已完成

- 已建立产品规划文档
- 已建立 AI 功能需求说明书
- 已建立 UI 设计文档
- 已建立技术架构设计文档
- 已建立 v1 技术选型决策文档
- 已初始化 Vue 3 + TypeScript + Vite 前端工程
- 已建立 Vue Router 路由结构
- 已建立 Pinia 状态管理
- 已实现本地小说项目的创建与打开
- 已实现不合法项目目录的检测与修复
- 已实现标准项目目录初始化
- 已实现 `novel.config.json` 读写
- 已实现 `prompts/system.md` 读取与保存
- 已实现项目文件扫描、分组展示与原文预览
- 已实现 LLM 配置测试连接
- 已实现 Embedding 配置测试连接
- 已实现 LLM 流式生成链路
- 已实现章节文件写入 `chapters/`
- 已明确 AI 模块接口与流式事件协议
- 已完成第一版 Claude Code 风格 Agent Loop 骨架
- 已实现模型驱动的 `ReadFile / EditFile / CreateFile / RenameFile / DeleteFile / ListDirectory / FindFiles` 文件工具调用链路
- 已移除 Bash 工具占位，当前 Agent 工具层不提供通用命令执行能力
- 已实现 OpenAI-compatible `tools / tool_calls` 流式解析
- 已实现工具结果回灌模型后的多轮 Query 循环
- 已建立项目级 JSONL 日志系统，日志写入 `.novel/logs/agent.log.jsonl`
- 已记录 Agent Run、Query Step、模型调用、工具调用、工具结果、项目打开/关闭/恢复等关键事件
- 已实现最近项目 IndexedDB 记忆与刷新后恢复
- 已在 Session Lab 中验证项目恢复、忘记记录、关闭项目、日志写入等行为

## 进行中

- 将现有 Session Lab 从测试台逐步演进为更接近 Claude Code REPL 的可观察执行界面
- 为写工具增加权限确认 / 暂停恢复机制
- 为 `ReadFile / EditFile` 补充读后状态记录与文件过期写入保护
- 为 Agent Loop 补充停止、AbortController、Query Guard 与队列策略
- 为后续 RAG 工具化保留稳定接口边界

## 待开始

### AI 能力

- 将 `RagSearch` 作为模型可主动调用的正式工具接入 Agent Loop
- 实现近期章节上下文的工具化读取策略
- 实现要素提取与写入
- 完善章节命名规范与生成结果结构化处理
- 增加写操作确认与文件 diff 预览

### RAG 能力

- 实现 Embedding 向量化
- 实现 Orama 索引与检索
- 实现生成时的粗检索复用
- 为 Rerank 精排与高级审校预留更完整的上下文接口

### 工程补充

- 完善错误处理与降级策略
- 设计正式工作台 UI 回接方案
- 设计 Git 自动提交策略
- 设计 Agent 会话持久化与上下文压缩策略

## 阻塞与风险

- 要素文件格式与提取协议尚未完全定稿
- 纯前端文件系统方案依赖浏览器能力，浏览器兼容性有限
- 写工具目前仍缺少 Claude Code 式权限确认机制，后续必须补齐
- `EditFile` 目前仍主要依赖 prompt 引导先读后改，尚未在工具层强制 `readFileState`
- RAG、要素提取、章节命名还未接入新的 Agent Loop 主链路
- 两阶段检索中的“Rerank 精排”效果需要在真实生成链路中验证
