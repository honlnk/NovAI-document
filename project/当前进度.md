# 当前进度

最后更新：2026-06-20

## 已完成

- 已建立产品规划文档
- 已建立 AI 功能需求说明书
- 已建立 UI 设计文档
- 已建立技术架构设计文档
- 已建立 v1 技术选型决策文档
- 已初始化 Vue 3 + TypeScript + Vite 前端工程
- 已建立 Vue Router 路由结构
- 已建立 Pinia 状态管理
- 已实现本地小说项目的创建与打开
- 已实现不合法项目目录的检测与修复
- 已实现标准项目目录初始化
- 已实现 `novel.config.json` 读写
- 已实现 `prompts/system.md` 读取与保存
- 已实现项目文件扫描、分组展示与原文预览
- 已实现 LLM 配置测试连接
- 已实现 Embedding 配置测试连接
- 已实现 LLM 流式生成链路
- 已实现章节文件写入 `chapters/`
- 已明确 AI 模块接口与流式事件协议
- 已完成第一版 Claude Code 风格 Agent Loop 骨架
- 已实现模型驱动的 `ReadFile / EditFile / CreateFile / RenameFile / DeleteFile / ListDirectory / FindFiles` 文件工具调用链路
- 已为 `ReadFile / EditFile` 接入 `readFileState` 与文件过期写入保护
- 已为文件工具补充 Vitest 行为测试，覆盖读取后编辑、新建、重命名与删除回收站
- 已移除 Bash 工具占位，当前 Agent 工具层不提供通用命令执行能力
- 已实现 OpenAI-compatible `tools / tool_calls` 流式解析
- 已实现工具结果回灌模型后的多轮 Query 循环
- 已建立项目级 JSONL 日志系统，日志写入 `.novel/logs/agent.log.jsonl`
- 已记录 Agent Run、Query Step、模型调用、工具调用、工具结果、项目打开/关闭/恢复等关键事件
- 已实现最近项目 IndexedDB 记忆与刷新后恢复
- 已在 Session Lab 中验证项目恢复、忘记记录、关闭项目、日志写入等行为
- 已完成第一版 UI 协作接口层 `packages/core/src/services/*`
- 已新增运行时项目注册表，UI 不再直接持有 `FileSystemDirectoryHandle`
- 已新增 `projectStore / settingsStore / chatStore` 对 service 的状态适配
- 已迁移 `SessionTestView`、`TestLabView` 使用 stores/services
- 已确认 `packages/app/src/views` 与 `packages/app/src/stores` 不再直接 import `packages/core/src/core/*`
- 已将接口层类型整理为显式 view type，减少 UI 对 core 内部类型的依赖
- 已完成正式 UI 页面骨架和路由（首页、主工作区、设置页）
- 已实现首页功能（项目列表、新建/打开项目、恢复上次项目、最近项目列表）
- 已实现左侧文件树（TreeNode 递归组件、文件夹展开/折叠、文件选择高亮、不同文件类型图标）
- 已实现中间对话面板（消息列表、用户/AI/系统消息展示、流式文本、多行输入）
- 已实现右侧面板（文件内容预览、预览/原始视图切换、文件信息展示、章节要素提取入口）
- 已实现设置页（LLM/Embedding/Rerank 配置、项目设置面板）
- 已实现 Toast 提示组件（success/error/info 类型、自动消失、动画效果）
- 已实现首次使用引导组件（引导配置 LLM API、可跳转设置页）
- 已实现文件树实时更新（AI 操作文件后自动刷新）
- 已实现选择文件时右侧面板自动展开；AI 生成后自动打开变更文件仍待补齐
- 已改进最近项目存储（支持保存最多 8 个最近项目列表）
- 已实现第一版章节要素提取链路：从章节正文中提取人物、地点、情节、时间线与世界观候选
- 已实现要素候选写入 `elements/characters`、`elements/locations`、`elements/plots`、`elements/timeline`、`elements/worldbuilding`
- 已在 Test Lab 与正式右侧内容面板接入“提取要素 / 写入 elements”操作入口
- 已在要素写入后标记 RAG 索引为 `stale`，提示后续重建索引
- 已为要素提取与写入补充 Vitest 行为测试
- 已将要素提取收敛为 **LLM 唯一路径**，移除临时正则降级提取器（正则会产生大量低质量误匹配，对创作有害无益）；LLM 不可用时直接报错并提示用户检查配置
- 已在正式设置页接入 RAG 向量索引状态查看与手动重建入口
- 已修复 Agent 将项目名误作为工具路径时的目录查找问题
- 已将 `RagSearch` 接入新的 Agent Loop，模型可主动调用 RAG 检索要素上下文
- 已在 `RagSearch` 内部复用现有向量召回、Rerank 与最终上下文筛选链路
- 已将章节正文写入策略收敛为 `chapters/*.txt`，并保留旧 `.md` 章节的读取、预览、计数和迁移改名兼容
- 已新增 `elements/entities/` 标准分组，扩展 element 类型、要素提取、写入、解析、RAG metadata、检索过滤和 UI 统计展示
- 已统一项目目录补齐策略：新建项目、打开/修复项目、恢复上次项目都会补齐 `elements/entities/`，项目检查会识别缺失的要素子目录
- 已为 Agent LLM 流式 tool_calls 解析异常和流式空闲超时补充非流式 fallback
- 已为 Agent LLM fallback 行为补充 Vitest 测试
- 已为 Agent Loop 增加运行中停止能力：LLM 流式请求接入 `AbortSignal`，停止时抛 `AgentAbortedError` 并保留已生成内容（不触发非流式 fallback），Loop 每轮与工具批次执行前检查停止信号，停止作为正常结束追加摘要并回退到 `waiting-user`；UI 发送按钮运行中切换为停止
- 已为 Agent 停止与工具批次中止行为补充 Vitest 测试（`llm.test.ts` / `tool-orchestration.test.ts`）
- 已将 `docs/` 重构为 `product / architecture / decisions / plans / project / archive` 分类目录，目录名使用英文，文档名保留中文
- 已将主工作区从「文件树 + 设置页」重构为「Activity Bar + 分类面板 + 对话驱动」模式（UI 重构 Phase R1）
- 已用 ActivityBar + CategoryPanel（章节 / 要素 / 提示词 / 对话占位）替换原 FileTreeSidebar
- 已将设置入口从整页路由 `/project/:id/settings` 改为模态框 SettingsModal，移除废弃路由
- 已为所有 API Key 输入框（LLM / Embedding / Rerank）接入可复用 PasswordInput，支持眼睛图标切换显隐
- 已为内容面板新增预览 / 编辑 / 原始三态分段控件（图标 + Tooltip），并支持编辑模式
- 已实现编辑模式：纯 textarea 编辑 `chapters/*`、`prompts/*`、`elements/*.md`，Ctrl/Cmd+S 手动保存 + dirty 标记，复用 File System Access writeFile
- 已实现选中内容引用：预览模式下选中文本生成引用 chip，作为独立 `quote` 字段透传给模型，用户气泡上方展示独立引用块，切换文件清空
- 已为支持引用注入在 core 层新增 `quote` 字段透传链路（chat.ts / types.ts / agent-service.ts / session.ts / prompt.ts），引用与用户意图在消息模型上解耦
- 已实现内容面板拖拽改宽（UI 重构 Phase R7）：新增 `ResizeHandle` 手柄，宽度可在 240~720px 区间拖拽、双击重置默认 320px，持久化到 `localStorage`，移动端（`lg:` 以下）不显示手柄
- 已实现 `@场景` 输入框指令（UI 重构 Phase R4）：输入 `@` 弹出场景选择弹层，支持筛选与键盘导航，选中即激活场景；输入框上方常驻绿色场景 chip，点 × 关闭，与提示词列表的激活标记双向联动
- 已实现 `/提取要素` 斜杠命令（UI 重构 Phase R6）：输入 `/` 弹出斜杠命令菜单（命令注册表驱动，可扩展），选中「提取要素」后展开章节多选，逐章提取并在对话流展示候选预览，确认后写入 `elements/*.md`；多章节提取时同类型同名候选项智能合并
- 已修复项目列表章节数恒为 0 的问题：打通 `chapterCount / elementCount` 数据流（`activateProject` 透传 → IndexedDB 持久化 → 首页卡片展示「N 章 · M 个要素」），首屏静默刷新已授权项目计数
- 已将 Agent 执行后的文件变更为工具层结构化输出（Agent 控制能力 Step 1）：写工具（CreateFile/EditFile/RenameFile/DeleteFile）通过新增的 `extractFileChange` 在成功 output 上声明 `FileChange`，经 `AgentToolResultMessage.fileChange` 与 `tool-result` 事件透传；`agent-service.collectChangedFiles` 改读结构化字段，删除 `isSuccessfulToolResult`/`extractTrashPath`/`collectToolResultTextById`/`toChangedFile` 等文本反推逻辑；`session.lastWrittenPath` 同步改读 `fileChange`；补 `extractFileChange` 与 `tool-execution` 透传 Vitest 测试
- 已为写工具接入写入前确认与 diff 预览（Agent 控制能力 Step 2/3，策略为每次写操作都确认）：`ToolDefinition` 新增 `buildConfirmation`，4 个写工具各自从 `validatedInput` 构造预览（edit/create 带文本 diff，rename/delete 路径级）；`tool-execution` 在 `run` 前拦截等待用户确认，拒绝时不执行并返回结构化拒绝结果回灌模型让其调整；`agent-service` 新增确认注册表 `confirmationMap` + `respondConfirmation`，确认等待期间复用停止信号中断；app 层 chat store 加 `pendingConfirmation` + `confirmWriteTool`/`rejectWriteTool`，新建 `WriteConfirmationCard.vue` 渲染 diff/路径预览，ChatPanel 挂载；同步清理 `confirmPendingFileChange`/`callTool` 全套旧残留与 `chat/tools.ts`（约 -529 行死代码）
- 已实现用户即时工具约束（Agent 控制能力 Step 5）：新增 `tool-policy.ts`，`parseToolPolicy` 从用户本轮指令解析「否定词 + 读/写动作词」组合得到 `ToolPolicy`（`{ allowRead, allowWrite }`）；采用软约束（`filterAvailableTools` 在 `query()` 入口把被禁工具从模型可见 schema 移除，`describeActivePolicy` 注入 user context）+ 硬约束（`executeAgentTool` 在参数校验后检查策略，命中直接返回失败 tool result 回灌模型）双层设计；用 `tool.isReadOnly` 二分判定，不硬编码工具名；补 `tool-policy.test.ts`
- 已实现 system prompt 同会话刷新（Agent 控制能力 Step 6）：`session.runChatTurn` 每轮运行前用 `buildAgentSystemPrompt` 重算本轮 system 内容，经 `hashContent`（FNV-1a，与 ReadFileState / RAG indexer 复用同一算法）比对 `session.systemPromptHash`，不一致时 `refreshSystemMessageContent` 原地替换 `agentMessages[0]`，`system.md` 或场景提示词任一变化都在下一轮自动反映；新增 `ChatSessionState.systemPromptHash` 字段；补 `session.test.ts`

## 进行中

- UI 重构 Phase R1-R7 已全部完成，详见 [UI 重构计划](../plans/UI重构计划.md)
- Agent 控制能力补强计划六个 Step 已全部完成，详见 [Agent 控制能力补强计划](../plans/Agent控制能力补强计划.md)
- 持续收敛 `services/types.ts`，后续按 project / agent / rag / settings 拆分接口类型
- 在真实创作任务中验证 Agent 主动 `RagSearch` 的触发率、召回质量与后续 `ReadFile(sourcePath)` 使用策略
- 继续细化 `chapters/*.txt` 的章节命名规范、生成结果结构化处理与旧项目批量整理工具
- 为 `characters / plots / timeline / entities` 补齐更稳定的默认模板与 Agent 写入规则

## 待开始

### AI 能力

- 将近期章节上下文接入当前模型驱动 Agent Loop，或明确作为工具化读取策略实现
- 完善要素提取质量、去重合并与覆盖策略
- 完善章节命名规范与生成结果结构化处理，适配 `txt` 章节正文

### RAG 能力

- 将当前检索实现升级为正式 Orama 召回链路
- 实现生成时的粗检索复用
- 完善索引过期提醒、自动增量重建与错误降级策略
- 为高级审校预留更完整的上下文接口

### 工程补充

- 完善错误处理与降级策略
- 设计 Git 自动提交策略
- 设计 Agent 会话持久化与上下文压缩策略

## 阻塞与风险

- 要素提取已收敛为 LLM 唯一路径，但缺少用户确认、去重合并与覆盖策略，后续仍需补齐
- 纯前端文件系统方案依赖浏览器能力，浏览器兼容性有限
- `RagSearch` 已接入新的 Agent Loop，但模型是否稳定主动调用仍需要通过正式项目真实创作验证
- 当前向量召回仍是 IndexedDB 记录 + 手写 cosineSimilarity 的过渡实现，正式 Orama 召回链路待补齐
- 两阶段检索中的“Rerank 精排”效果需要在真实生成链路中继续验证
